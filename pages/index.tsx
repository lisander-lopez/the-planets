import type { NextPage } from 'next'
import Head from 'next/head'
import Image from 'next/image'

const Home: NextPage = () => {
  return (
    <div className={``}>
      <h1>Test H1</h1>
      <a className={`btn-primary`}>Test</a>
      <button className={`btn-primary`}>Test</button>

    </div>
  )
}

export default Home
